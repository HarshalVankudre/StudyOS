// Test stub for Next.js's `server-only` package, which has no importable build
// outside the React Server Component graph. Server modules under test import it
// purely as a client-bundle guard; in unit tests it is a no-op.
export {};
